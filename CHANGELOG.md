# CHANGELOG

## 2026-09-24
- feat(net): QUEUE carries a duel match_token; server pairs same-token connections ahead of FIFO (S537 Duel Phase 2, Phase 2) -- `QUEUE`'s payload grows to an optional 33rd/34th-byte form (`same_deck` + a 32-ASCII-char token), purely additive over wire protocol v3 (`core/protocol.h`/`.c`) -- **no proto version bump**, an old client that never sends the new form behaves exactly as before, same shape as `same_deck` itself. `dw_server` (`apps/server/main.c`) gained `find_token_pair()`, checked before its existing FIFO/bot logic in `try_pair_mode()`: two queued connections in the same mode presenting an identical token are matched with each other specifically. Found and fixed a real design bug while building this, not just adding the happy path: a naive version let a lone token-holder get FIFO-matched with an unrelated stranger the moment queue size hit 2, if their actual duel partner hadn't connected yet -- `oldest_queued()` now excludes any connection carrying a match_token from ordinary FIFO/bot pairing entirely, so it only ever pairs with its specific partner (or waits). Verified two ways: new `test_protocol.c` coverage (round-trip + malformed-length cases for the new QUEUE form) and a live e2e scratch test driving real `dw_server`+`dw_client` processes over real TCP -- proved the actual claim (a lone token-holder does NOT get swept into a stranger's match while waiting), not just that two token-holders queuing alone would pair (FIFO would do that anyway). `dw_client` gained `--match-token` for this kind of testing; `core/client.h`/`.c`'s `DwRunOpts.match_token` is the real, minimal hook (dw_bot/GUI/web still need their own Phase 3 wiring, not built here). Full `scripts/build.sh --gui` suite green, no regressions. This is server-side only -- no client (native, web, WOTAN) surfaces a "queue for this duel" affordance yet, that's Phase 3. (sess-20260923-1030-4a526255)
- feat(gui): Friends & Duels screen in the native SDL2 client (S537 continued) -- the C GUI (`apps/gui/main.c`) gets the same profile/friends/friend-requests/duels UI the web client and WOTAN already have, reachable from the Main Menu via a new "FRIENDS & DUELS" button (shown once an account is claimed). New `core/iduna.c`/`.h`: 9 `dwi_*` functions (`dwi_profile`, `dwi_friend_request_send`, `dwi_friend_requests_list`, `dwi_friend_request_respond`, `dwi_friends_list`, `dwi_friend_remove`, `dwi_duel_create`, `dwi_duels_list`, `dwi_duel_respond`) against IDUNA's S537 routes. These needed a genuinely new primitive: the client's existing JSON helpers (`dw_json_str`/`dw_json_int`) only ever read a single named field, never a list of objects -- added `dw_json_array_at()` to `core/http.c`/`.h` (quote/brace-tracked array walk, supports a bare top-level array via `key=NULL`) so `dwi_friends_list`/`dwi_duels_list`/`dwi_friend_requests_list` can iterate real multi-item responses without a full JSON parser. New `S_SOCIAL` screen (`apps/gui/main.c`) with 3 tabs (FRIENDS/REQUESTS/DUELS), an add-friend-by-ID field, and per-row accept/decline/remove/challenge buttons, following the same bitmap-font/`click()`/`key()`/`SDL_TEXTINPUT`-gating pattern every other screen already uses. Verified two ways: a standalone unit test against a controlled fake IDUNA (10/10 assertions, including the two-incoming/one-outgoing and bare-top-level-array parsing cases, ASan/UBSan clean), and a live, real, Xvfb+xdotool screenshot pass of the actual compiled `dw_gui` binary (menu button, all 3 tabs showing correct data and correct conditional button visibility). Full `scripts/build.sh --gui` suite green -- no regressions. **Honest gap**: keyboard text entry into the add-friend field wasn't visually confirmed in the screenshot pass -- `xdotool type --window` under a window-manager-less Xvfb doesn't reliably establish real X11 input focus the way mouse clicks do; the underlying `append_to_social_field`/`SDL_TEXTINPUT` dispatch is structurally identical to the already-shipped `append_to_claim_field`/`S_CLAIM` code this same harness limitation would equally affect, so this is a test-harness gap, not a suspected app bug. This closes SECTION 537's "native-client half"; Duel Phase 2 (accepted duel -> live match) is still open. (sess-20260923-1030-4a526255)
- feat(web): Friends & Duels panel in the browser client (S537 continued) -- new `web/src/social.ts` (friend-requests, friends, duels against IDUNA's `/api/v1/games/deadweight/...` routes) + a new "Friends & Duels" section in `index.html`/`main.ts`, shown once an account is bootstrapped. Works for any account (guest or email-linked), the same identity/routes WOTAN's `friends.html`/`profile.html` already use. `tsc` strict-mode clean, JS syntax-checked, headless-Chrome screenshot of the page shell. Not live-clicked-through yet -- IDUNA's new routes aren't deployed, and this session's own signup rate cap was already spent verifying the account-wiring piece earlier today. (sess-20260923-1030-4a526255)
- feat(web): real IDUNA account wiring for the browser client (S537) -- new `web/src/account.ts` (guest-register/guest-login refresh/guest-upgrade "link email", mirroring `apps/gui/main.c`'s own shipped account flow), `client.ts`'s `connect()` now takes a real token and passes it into `HELLO` (was always `''`), `index.html`/`main.ts` add an IDUNA base URL field, account status line, and a "Link email" form. Closes the real, previously-honest "No IDUNA auth" gap in `web/README.md`. Live-verified against the real running IDUNA (127.0.0.1:8080): a fresh guest account is real and a second bootstrap call correctly reuses the same `player_id` via `guest-login`. Not verified this pass: an auth-required `dw_server` actually accepting a real web-client token over `HELLO` -- IDUNA's real per-IP daily signup cap was hit by repeated test registrations, and bypassing it (even locally) was correctly refused as security-weakening; `encodeHello`'s wire format itself is unchanged, already-proven code. Also still missing: CORS on IDUNA's `/api/v1/games/` routes for a real cross-origin browser deployment. `tsc` clean. See `web/README.md` for the full honest account. (sess-20260923-1030-4a526255)
- docs(NORTHSTAR): logged S537 -- IDUNA online accounts + social features (profiles/friends/friendly-challenge duels), founder real-time. Real work landed in IDUNA (`game_social.go`, new `/api/v1/games/deadweight/{friend-requests,friends,duels,players/{id}/profile}` routes, commit 70b6b06) and WOTAN (`friends.html`/`profile.html`, commit 46e3ab0) -- no DEADWEIGHT repo code changed this pass. Real, named, deferred here: wiring `web/src/client.ts` itself to real accounts (still `--no-auth server` today), in-game friends/duel UI, and turning an accepted duel into a live match (Duel Phase 2). See `NORTHSTAR.md`'s own new update section and `EMILY/BACKLOG.md` SECTION 537. (sess-20260923-1030-4a526255)

## 2026-09-22
- CI now signs + publishes release binaries to IDUNA's new app-release registry (guarded no-op until GitHub secrets are configured). (sess-20260920-1908-24cb3558)

- New scripts/onboarding_screenshots.sh -- automates real, live screenshot capture of the onboarding funnel (menu, Claim Account modal, queue, match, end) via Xvfb+xdotool driving the actual dw_gui binary. Kanban #422. (sess-20260920-1908-24cb3558)


## 2026-09-21
- New Claim Account modal (brutalist, labeled EMAIL/PASSWORD, gated on real Guest state via account_state) replaces the old always-visible unlabeled inline fields; fixed a real bug where maximizing/fullscreening the window left content pinned top-left instead of centered; added missing '@'/'*' glyphs to the bitmap font (email/password fields were unreadable) (sess-20260920-1908-24cb3558)
- Fix: production dw_server has run with --no-auth and no IDUNA config since it was first deployed -- match results, ELO stats, and draft win-loss tracking were never once reported to IDUNA. Wired in the real (already-provisioned) DEADWEIGHT-SERVER/DEADWEIGHT-BOTS agent credentials, redeployed, and verified end to end against production (a real match's loss correctly incremented a test account's draft run). (sess-20260920-1908-24cb3558)
- Fix: redeem-code input buffer was 23 chars, truncating every real 29-char claim code before it was sent -- unlocking Premium/redeeming codes never worked. Also fixed stale ticket-balance display (to_menu never refreshed it) and a hardcoded hub_active=1 bug that ignored the server's real draft-run state after a run ended. (sess-20260920-1908-24cb3558)
- Fix: live match/deck logs (dw-server's --match-log) moved out of the git working tree to ~/.local/var/deadweight/matches -- the in-repo path was being unknowingly truncated by routine dev repo-hygiene commands, which caused the WOTAN deck-stats/player-deck data loss reported 2026-09-21 (sess-20260920-1908-24cb3558)
- Fix: new client's guest auth no longer mislabels a 429 rate-limit rejection as 'IDUNA offline' -- surfaces the real HTTP status so a signup-cap hit is diagnosable, not mistaken for an outage (sess-20260920-1908-24cb3558)
- S512: zero-friction boot (no Host/Port/Name UI, real loading screen), server-assigned lore names, live-verified 20-ticket grant (sess-20260920-1908-24cb3558)
- S510: real ticket-consuming Draft Runs -- IDUNA cash-out reward table + Abort/Resume endpoints, new Draft Hub GUI screen, DW_C_DRAFT_RESUME wire message with server-side deck-legality revalidation (sess-20260920-1908-24cb3558)

- feat(net): real TLS for the Windows build too — `dw_gui.exe` cross-compiles clean with mbedTLS + an embedded CA bundle (S508f) -- continuing S508e's Linux TLS work: cross-compiled mbedTLS from source for `x86_64-w64-mingw32` (its own plain `Makefile` supports `CC`/`AR` overrides, no CMake needed — see `docs/WINDOWS_TLS_BUILD.md` for the full, reproducible steps), and closed the real, found-live gap that PARENA's own TLS block had accidentally been dead code on Windows (nested inside `net/tcp.prn`'s `#ifndef _WIN32` guard — see PARENA commit `169e7e5`). New `scripts/gen_ca_bundle.sh` + checked-in `core/runtime/ca_bundle_data.h` (a real system CA bundle embedded directly into the binary — Windows has no single-file bundle at a fixed path the way Linux's `ca-certificates` package does). `scripts/build.sh --gui --windows` now auto-detects a real mbedTLS mingw build (`MBEDTLS_MINGW_DIR`, same env-var-override convention as `SDL2_MINGW`) and links real TLS support into `dw_gui.exe` when found. Verified: valid `PE32+` binary, zero undefined symbols, 146 real `-----BEGIN CERTIFICATE-----` markers confirmed embedded via `strings`. **Honestly NOT runtime-verified** — no Wine/Windows environment in this sandbox to actually execute the `.exe` and prove a live handshake; `docs/WINDOWS_TLS_BUILD.md` states this plainly rather than implying parity with the Linux build's own live verification.

- feat(net): real TLS support via PARENA's mbedTLS FFI binding, client now defaults to the real production IDUNA URL (S508e) -- founder real-time asked to package a release client pointed at production; found live that the client had ZERO TLS support (`dw_parse_url` explicitly rejected `https://`) and defaulted to `http://localhost:8080`, which would have shipped a build that couldn't reach anything on a real player's machine. Explicit pushback given and accepted before any crypto code was written: never hand-roll TLS in an immature DSL -- real TLS is bound via PARENA's new `stdlib/net/tls.prn` (mbedTLS FFI, see PARENA/CHANGELOG.md same date), never reimplemented here. `core/http.c`'s `dw_http`/`dw_parse_url` gained a real `use_tls` path (`https://` now parses, defaults to port 443) that fails clean (never silently falls back to plaintext) when a binary wasn't built with `PARENA_WITH_TLS`. `core/iduna.h`'s `DwIduna` gained `use_tls`, threaded through all 12 `dw_http` call sites in `core/iduna.c`. `scripts/build.sh --gui` auto-detects mbedTLS + a sibling PARENA checkout and links real TLS support in when found (opt-in, not hard-required -- `dw_gui` still builds without it). `A.iduna_url`'s default is now `https://okemily.com`, the real production front door. Two real, found-live glibc feature-test-macro conflicts fixed along the way (`_POSIX_C_SOURCE`/`_DEFAULT_SOURCE` redefinition errors under `-Werror`, from `core/net.h` and PARENA's own runtime header disagreeing on what was already defined -- both now `#ifndef`-guarded, never clobber an already-set value). **Live-verified with the actual shipped binary**, no override flags: `dw_gui --autostart` against its own real default config performed a real TLS 1.2 handshake with real certificate verification against `https://okemily.com`, registered a real guest account, got a real ticket balance back. Full `scripts/build.sh --gui` suite green.

  **Honest, named gap: Windows/mingw cross-build is NOT done.** No `mingw-w64` mbedTLS package exists in apt; a real cross-compile would need mbedTLS vendored from source and built via CMake against the mingw toolchain, and even then this sandbox has no way to actually run a compiled `.exe` to verify a real handshake -- shipping unverified crypto code for a platform that can't be tested here was a deliberate call not to rush. The Linux client is real, tested, and production-ready now; Windows needs that separate, scoped pass before it can honestly be called done.

- feat(gui): OS clipboard paste support for menu text fields (S508d) -- founder real-time: "ensure the text input box fully supports OS-level copy/paste so players do not have to type it manually" (the new 29-character dashed claim-code format, see IDUNA/CHANGELOG.md same date). SDL2 doesn't wire OS clipboard paste into `SDL_TEXTINPUT` automatically -- added explicit Ctrl+V handling (`SDL_GetModState() & KMOD_CTRL`, `SDL_GetClipboardText()`) applying the same character-filter rule typing already uses, refactored into a shared `append_to_field()` both paths now call. Works on every menu field (name/host/port/redeem code/email/password), not just redeem. Full `scripts/build.sh --gui` suite green.

- feat(net): Uncapped Draft Run + guest-to-email account linking client functions, real match-result mode bug fix (S508c) -- founder real-time economy correction: "1 Ticket = 1 Draft Run... no win cap... ends strictly at 3 losses" plus "Link Email (Save Progress)." `core/iduna.c/.h` gained `dwi_draft_run_start`/`dwi_guest_upgrade`/`dwi_email_login` against IDUNA's new routes (see IDUNA/CHANGELOG.md same date). Found and fixed a real, load-bearing bug along the way: `DwMatchReport` had no `mode` field at all, and `dwi_report` hardcoded `"mode":0` in every match-result POST -- every historical draft match ever reported to IDUNA was misreported as card mode, silently, since `game_matches.mode` (and now the new Uncapped Draft Run tracking, which reads this same field to know which matches count) both depend on it being correct. `apps/server/main.c` now threads the real `mt->mode` through. GUI (`apps/gui/main.c`) gained a "LINK EMAIL (SAVE PROGRESS)" text-input flow next to the existing Redeem Code box, wired to `dwi_guest_upgrade`, keeping the same `player_id`/ticket balance/auth token across the upgrade. **Honest scope, not silently skipped**: `dwi_draft_run_start` is NOT yet called from `apps/server/main.c`'s poll loop -- doing so synchronously would block the server's single-threaded event loop for up to the HTTP timeout; the correct design is a new async job type following the same worker-thread pattern `J_VERIFY`/`J_REPORT` already use, real, scoped, separate follow-up work. Full `scripts/build.sh --gui` suite (ASan/UBSan unit tests, 4 e2e scenarios including the fake-IDUNA match-result contract test, GUI selftest, GUI draft selftest, FX demo) green with the mode-passthrough fix in place -- confirmed the existing e2e's own `mode == 0` assertion holds because those matches genuinely are mode 0, not coincidentally.

  **Re-flagging the Phase 3 orders (`redraw_cooldown_remaining`, `npc_controlled`)**: still not built, same reason given last time -- `ArenaHero`/`arena_npc_hero_tick`/`card_battler.c` exist only in `GoblinFoxDragon/apps2/battlegrounds_gui`, not this repo. DEADWEIGHT has no hero/NPC-piloted unit, and hand refill (`core/match.c`) is instant/round-synchronous, not a real-time per-slot cooldown timer. See `EMILY/BACKLOG.md` SECTION 508.

- feat(gui): Itch.io survival-launch Main Menu -- zero-friction IDUNA auth, DRAFT/PRACTICE, Redeem Code (S508) -- founder real-time pivot: launch on Itch.io before Steam, "skip Phase 1 (Steam Auth)... wire the Main Menu (Phase 2) to use IDUNA's existing authentication." `apps/gui/main.c` now calls `dwi_guest_register`/`dwi_guest_login` automatically on boot (persisted to `--account FILE`, default `dw_account.txt`; re-launch reuses the same account, confirmed live -- no duplicate registration), filling the existing `--token`/AUTH flow to `dw_server` in unmodified (an explicit `--token`/`DW_TOKEN` still overrides, for `--no-auth` server testing). Menu redesigned: persistent `TICKETS: N` (+ `FOUNDER` badge) counter top-right, the old single PLAY button replaced with `DRAFT (COST: 1 TICKET)` (disabled client-side at 0 tickets -- real server-side enforcement is still Phase 4, not built) and `PRACTICE (RANDOM DECK, FREE)`, plus a Redeem Code text box + button (`dwi_redeem`, new). New `core/http.c`'s `dw_json_int` reused from the earlier Steam work; `core/iduna.c` gained `dwi_redeem`. `--selftest`/`--fx-demo` explicitly skip the new IDUNA bootstrap (CI has no IDUNA reachable and must stay fast/network-free -- confirmed no regression, both still pass). Live-verified end to end against real IDUNA + `dw_server` binaries (not just unit tests): first boot registers a real guest account and shows `tickets=1` (the daily freebie), a second boot reuses the same account with no double-grant, and a real `/redeem` call against the same running IDUNA instance correctly credited 7 tickets + the founder flag.

  **Honest architecture correction, found while scoping**: the earlier Phase 3 orders (`redraw_cooldown_remaining`, `npc_controlled` wire fields) describe a different game -- `ArenaHero`/`arena_npc_hero_tick`/`card_battler.c` exist only in `GoblinFoxDragon/apps2/battlegrounds_gui`, not here. DEADWEIGHT has no hero/NPC-piloted unit at all, and hand refill (`core/match.c`) is instant/round-synchronous, not a real-time per-slot cooldown timer -- there is nothing for either named field to represent in this game's actual design. Not built, flagged instead of faked; see `EMILY/BACKLOG.md` SECTION 508.

- feat(net): Steam login + Draft-ticket client functions (S507, Phase 1 of the Steam F2P pivot) -- `core/iduna.c`/`.h` gained `dwi_steam_login`/`dwi_ticket_balance`/`dwi_ticket_consume`, mirroring `dwi_guest_login`/`dwi_report`'s existing shape against IDUNA's new `/api/v1/games/deadweight/steam-login`/`players/{id}/tickets`/`tickets/consume` routes (see IDUNA/CHANGELOG.md same date). New `dw_json_int` helper in `core/http.c`/`.h` (int/bool JSON field extraction, mirroring `dw_json_str`). HONEST scope: these functions only speak the IDUNA HTTP contract -- obtaining the hex auth ticket itself is real client-side Steamworks SDK work (`ISteamUser::GetAuthSessionTicket`) that is NOT done here: the SDK is proprietary/Valve-partner-gated and not present in this sandbox (same class of external gap as `MJOLNIR`/`SPIDERBEETLE`'s own honestly-named Android SDK absence). `scripts/build.sh` full suite (ASan/UBSan unit tests + 4 real e2e scenarios) green with the new code linked into `dw_server`/`dw_client`/`dw_bot`/`dw_gui`.

- feat(fx): unified Windows + browser round-resolution animation/audio decision layer via new PARENA/stdlib/deadweight/fx_rules.prn -- founder real-time: "use parena to unify the windows and TS versions, windows is canonical, TS should copy it exactly... dogfood it, eat more of the app." apps/gui/fx.c's fx_begin()/begin_stages() (scenario classification, winner/crit, timeline stage timings, audio cue selection) refactored to CALL the generated core/fx_rules.c (via new core/fx_rules.h, same `-include card_rules.h` convention core/bot_brain.c already uses) instead of hand-deriving that logic -- Windows is now a real consumer of the shared source, not a second independent implementation. web/src/fx.ts (new) calls the same logic compiled to web/src/generated/FxRules.ts and renders it with Canvas2D + Web Audio oscillators, wired into web/src/main.ts's onRoundResult. New tests/test_fx_rules.c (4092 checks against an independent, hand-typed oracle transcribed from the pre-refactor C logic), wired into scripts/build.sh's always-on suite. `scripts/build.sh --gui` verified clean end-to-end (all existing test_sfx/test_gui_selftest/fx-demo assertions still pass -- zero behavioral regression from the refactor). Browser side verified via a real live match: web/bridge/e2e_test.mjs now also computes fx.computeTimeline() for every real ROUND_RESULT and sanity-checks the output (9/9 rounds, 0 failures). HONEST gap: the browser's Canvas2D/Web Audio rendering has never been opened in an actual browser window in this sandbox (no display); energy-delta and burn/regen status visuals are named-but-not-wired since ROUND_RESULT itself carries neither field -- see web/README.md and docs/ANIMATION_AND_AUDIO.md. A real logic bug in fx_rules.prn's first draft (fx-scenario hardcoded BLITZ for one branch) was caught by a 64-case cross-check against the original C logic before this ever reached fx.c -- see PARENA/CHANGELOG.md
- feat(web): real, working browser client (VS0.5-web, dev) dogfooding PARENA's TypeScript emitter -- card_rules.prn now also compiles to web/src/generated/CardRules.ts (scripts/gen_rules.sh), a hand-written WebSocket wire-protocol client (web/src/proto.ts, web/src/client.ts) speaks docs/WIRE_PROTOCOL.md directly, and a dumb WS<->TCP bridge (web/bridge/ws-tcp-bridge.js) reaches the real dw_server (still TCP-only). Verified end-to-end: a real 11-round match played through the compiled client against a live bot (web/bridge/e2e_test.mjs). Random queue only, no auth, no animation -- see web/README.md. Found and fixed two real, load-bearing PARENA TypeScript-emitter bugs along the way (I32 division truncation, a 511-char tb_appendf buffer overflow) plus a `(not x)` gap -- see PARENA/STDLIB.md
- docs(readme): full marketing rewrite of README.md for the card battler (triangle, keywords, draft/random queues, real online multiplayer from day one); backpack battler/VS1 mentioned only as the closing line, explicitly flagged as not built yet; honest-status section preserved from the prior README (draft not deployed live, Android draft screens untested on device, constructed decks not built)

## 2026-09-20

- EMP visual effect redesigned for photosensitivity safety (pulsed flicker + scanlines + glitch bars); text drop shadows added for contrast (sess-20260920-1908-24cb3558)


## 2026-09-19
- feat(gui): round-resolution animations + synthesized audio in the SDL2 client (fx.c/sfx.c), --fx-demo, test_sfx, docs/ANIMATION_AND_AUDIO.md; Windows/Linux only (sess-20260918-1725-497f394f)
- ops: deployed the retheme (server + bots, proto 3); archived pre-retheme deck/match logs; server continues deck ids across restarts (sess-20260918-1725-497f394f)
- feat: Offense/Operations/Defense retheme -- 105 cards (35 per colour), Operations keywords Lock/Sabotage/Flank/Scan/Siphon, wire proto 3, legacy Burst/Tank/Shield terms removed; not deployed (sess-20260918-1725-497f394f)
- feat: scripts/export_cards.sh + apps/tools/cards_json.c -- card catalog JSON for WOTAN's deck browser (sess-20260918-1725-497f394f)
- feat(draft): draft mode -- separate queue, 16-pick 23-card deck, same-deck/redraft (bots keep winners), decks.ndjson log; server, bots, Windows GUI, Android; not deployed yet (sess-20260918-1725-497f394f)
- fix(training): Colab script/notebook clone the public repo over plain https -- no GitHub token prompt (sess-20260918-1725-497f394f)
- docs(training): shared registry live on okemily.com (IDUNA redeployed, DEADWEIGHT-RL provisioned); real push + resume verified (sess-20260918-1725-497f394f)
- feat(gui): card effect text always visible on every card (hand 2x2 grid, revealed cards too), 2x text with word wrap, falls back to 1x if it doesn't fit; effectless cards show no text; window 480x956; added & ' ; $ = glyphs; verified via headless selftest frames (sess-20260918-1725-497f394f)
- feat(gui): Windows client defaults to okemily.com:6980 (was 127.0.0.1:7700) (sess-20260918-1725-497f394f)
- feat(training): training/colab_train.py one-cell Colab bootstrap + dw_train --resume-from-registry; first real MaskablePPO smoke run + resume verified locally; live IDUNA registry route not deployed yet (sess-20260918-1725-497f394f)
- feat: round limit 8 -> 100, round timer 20s -> 40s; server play-log buffer sized for 100 rounds (was fixed 8) (sess-20260918-1725-497f394f)

- feat(card-mode): 64 new cards (73 total) -- armor, credits, statuses, hand locks, cancel/copy/swap control, Dark Pool; wire protocol v2; data-driven effect engine in PARENA; C==Java==Python parity over ~14.7k vectors; PARENA Java emitter truncation bug fixed (PARENA 8141f8f). Draft/deckbuilding is next (not in this ship) (sess-20260918-1725-497f394f)


## 2026-09-18
- docs(card_expansion): graded Haiku's card-expansion proposal + meta prompt, wrote proposal review, metaprompt engineering pass, and v2 meta prompt (not yet run against a model) (sess-20260918-1725-497f394f)
- fix(android): LOCK IN crashed the app -- Session sent frames on the UI thread (NetworkOnMainThreadException); all outbound writes now go through a single writer thread + regression test (sess-20260918-1725-497f394f)
- S503-14b: dw_bot hybrid brain -- hand-written MLP (PARENA bot_brain.prn -> core/bot_brain.c) blended with heuristic archetype prior; distilled default weights embedded; --brain/--weights/--wh/--wn/--sigma; tests + win-rate table (sess-20260918-1725-497f394f)
- S503-09: dw_gui SDL2 card client (Windows+Linux), build.sh --gui, headless selftest, CI gui job + release packaging (sess-20260918-1725-497f394f)
- S503-10: NOCK-built card art (art/build_art.sh) wired into CardView with shape fallback (sess-20260918-1725-497f394f)
- training: AUTH frame (0x06) in codec/env/fake server; integration + dry-run trainer verified against real dw_server; fixed ghost-queue pairing on opponent shutdown (sess-20260918-1725-497f394f)
- Android: AUTH frame for full IDUNA tokens, 1024 max frame, integration test vs real dw_server+dw_bot (S503-07) (sess-20260918-1725-497f394f)
- lane B: wire protocol codec + AUTH frame, deterministic match core, dw_server/dw_client/dw_bot, 3-bot pool, e2e under ASan/UBSan, IDUNA auth+result reporting, systemd unit files (S503-04/05) (sess-20260918-1725-497f394f)
- training/: PFSP 3-role league (ported), game-scoped registry client, wire-protocol env + fake server, dw_train dry-run, Colab stub (S503-08) (sess-20260918-1725-497f394f)
- Android VS0: plain-JVM core (codec/transport/session/model), programmatic card UI, IDUNA guest auth hook, 118-check CoreTest; APK builds clean (S503-07) (sess-20260918-1725-497f394f)
- VS0 scoping pass (`docs/VS0_SCOPING.md`, `CARD_MODE_RULES.md`, `WIRE_PROTOCOL.md`): card mode first, Android-first,
  bots + multiplayer from day one, backpack battler = VS1. (S503-01)
- Clean-build skeleton: PARENA-generated `card_rules` (C + Java from one `.prn`), 1610 cross-target parity vectors +
  hand-typed oracle test, `dw_server`/`dw_client` stubs, Windows mingw cross-build, Bazel APK, CI with auto minor
  releases. Local `scripts/build.sh --all` clean. (S503-02/03)
